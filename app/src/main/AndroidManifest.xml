<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.internshipproject">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".MakeServiceCall"
            tools:ignore="Instantiatable" />
        <activity android:name=".ConnectionDetector1"
            tools:ignore="Instantiatable" />
        <activity android:name=".callActivity" />
        <activity android:name=".statusActivity" />
        <activity android:name=".chatActivity" />
        <activity android:name=".msgActivity" />
        <activity android:name=".custom_pageActivity" />
        <activity android:name=".Product_detailActivity" />
        <activity android:name=".ShoppingActivity" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".activity_login" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>